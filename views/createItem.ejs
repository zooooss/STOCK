<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
        <link rel="stylesheet" href="/main.css" />
        <title>Create Item</title>
        <style>
            * {
                box-sizing: border-box;
                font-family: 'Inter', sans-serif;
            }
            body {
                background: white;
                margin: 0;
                display: flex;
                flex-direction: column;
                align-items: center;
                padding-bottom: 50px;
            }

            .container {
                width: 100%;
                max-width: 480px;
                padding: 0 20px;
            }

            .info-bar {
                width: 100%;
                max-width: 480px;
                background-color: #1a1b2e;
                color: white;
                padding: 12px 20px;
                font-weight: 600;
                font-size: 14px;
                display: flex;
                justify-content: space-between;
                align-items: center;
                border-radius: 8px;
                margin-top: 10px;
                margin-bottom: 20px;
            }

            .form-group {
                margin-bottom: 20px;
            }
            .form-label {
                display: block;
                font-size: 11px;
                font-weight: 800;
                margin-bottom: 8px;
                color: #111;
                text-transform: uppercase;
            }

            .form-input {
                width: 100%;
                background: #f2f4f6;
                border: none;
                padding: 15px;
                border-radius: 8px;
                font-size: 14px;
                color: #333;
                outline: none;
            }
            .form-input::placeholder {
                color: #aaa;
                font-weight: 500;
            }

            select.form-input {
                appearance: none;
                background-image: url("data:image/svg+xml;charset=UTF-8,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='%23aaa' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'%3e%3cpolyline points='6 9 12 15 18 9'%3e%3c/polyline%3e%3c/svg%3e");
                background-repeat: no-repeat;
                background-position: right 15px center;
                background-size: 12px;
            }

            .action-buttons {
                margin-top: 30px;
                display: flex;
                flex-direction: column;
                gap: 12px;
            }

            .btn {
                width: 100%;
                padding: 16px;
                border: none;
                border-radius: 8px;
                font-weight: 800;
                font-size: 14px;
                cursor: pointer;
                display: flex;
                justify-content: center;
                align-items: center;
            }

            .btn-navy {
                background: #1a1b2e;
                color: white;
            }
            .btn-grey {
                background: #b0b0b0;
                color: #111;
                justify-content: center;
                padding: 0 20px;
                height: 50px;
            }
            .btn-red {
                background: #a33030;
                color: white;
                justify-content: center;
                padding: 0 20px;
                height: 50px;
                text-decoration: none;
            }

            .arrow {
                font-size: 18px;
                font-weight: 400;
            }

            /* ✅ 팝업 모달 스타일 */
            .modal-overlay {
                display: none;
                position: fixed;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: rgba(0, 0, 0, 0.5);
                z-index: 1000;
                justify-content: center;
                align-items: center;
            }

            .modal-overlay.active {
                display: flex;
            }

            .modal-content {
                background: white;
                border-radius: 16px;
                width: 90%;
                max-width: 400px;
                padding: 30px 20px;
                position: relative;
            }

            .modal-close {
                position: absolute;
                top: 15px;
                right: 15px;
                background: none;
                border: none;
                font-size: 24px;
                font-weight: 700;
                cursor: pointer;
                color: #333;
            }

            .modal-title {
                text-align: center;
                font-size: 18px;
                font-weight: 800;
                margin-bottom: 30px;
                color: #111;
            }

            .modal-buttons {
                display: flex;
                gap: 10px;
            }

            .modal-btn {
                flex: 1;
                background: #1a1b2e;
                color: white;
                border: none;
                padding: 40px 20px;
                border-radius: 12px;
                font-weight: 700;
                font-size: 13px;
                cursor: pointer;
                text-align: center;
                line-height: 1.4;
            }

            .modal-btn:hover {
                background: #2a2b3e;
            }

            /* ✅ 숨겨진 파일 입력 */
            .hidden-file-input {
                display: none;
            }
        </style>
    </head>
    <body>
        <%- include('header.ejs') %>

        <div class="info-bar">
            <span><%= orderCount %> ITEMS</span>
            <span>▼</span>
        </div>

        <div class="container">
            <form action="/orderlist/add" method="POST" enctype="multipart/form-data">
                <div class="form-group">
                    <label class="form-label">Item Name*</label>
                    <input type="text" name="itemName" class="form-input" placeholder="ITEM NAME" required />
                </div>

                <div class="form-group">
                    <label class="form-label">Supplier*</label>
                    <input type="text" name="supplier" class="form-input" placeholder="SUPPLIER" required />
                </div>

                <div class="form-group">
                    <label class="form-label">SKU# If Available</label>
                    <input type="text" name="sku" class="form-input" placeholder="SKU#" />
                </div>

                <div class="form-group">
                    <label class="form-label">Price If Available</label>
                    <input type="number" step="0.01" name="price" class="form-input" placeholder="PRICE" />
                </div>

                <div class="form-group">
                    <label class="form-label">Pack Unit</label>
                    <select name="unit" class="form-input">
                        <option value="" disabled selected>KG, BOX, BOTTLE</option>
                        <option value="KG">KG</option>
                        <option value="LB">LB</option>
                        <option value="BOX">BOX</option>
                        <option value="BOTTLE">BOTTLE</option>
                        <option value="PACK">PACK</option>
                        <option value="BAG">BAG</option>
                        <option value="WHOLE">WHOLE</option>
                    </select>
                </div>

                <div class="form-group">
                    <label class="form-label">Category</label>
                    <input type="text" name="category" class="form-input" placeholder="KITCHEN, SUSHI, BAR, TAKEOUT" />
                </div>

                <!-- ✅ 숨겨진 파일 입력 -->
                <input type="file" id="galleryInput" name="itemImage" accept="image/*" class="hidden-file-input" />
                <input
                    type="file"
                    id="cameraInput"
                    name="itemImage"
                    accept="image/*"
                    capture="environment"
                    class="hidden-file-input"
                />

                <div class="action-buttons">
                    <!-- ✅ ADD IMAGE 버튼 - 다른 버튼들과 동일한 스타일 -->
                    <button type="button" class="btn btn-navy" onclick="openImageModal()">ADD IMAGE +</button>

                    <button type="submit" name="action" value="addmore" class="btn btn-navy">ADD MORE +</button>

                    <button type="submit" name="action" value="save" class="btn btn-grey">
                        <span>SAVE & BACK</span>
                        <span class="arrow">→</span>
                    </button>

                    <a href="/orderlist" class="btn btn-red">
                        <span>CANCEL & BACK</span>
                        <span class="arrow">→</span>
                    </a>
                </div>
            </form>
        </div>

        <!-- ✅ 이미지 선택 모달 -->
        <div class="modal-overlay" id="imageModal">
            <div class="modal-content">
                <button class="modal-close" onclick="closeImageModal()">✕</button>
                <div class="modal-title">ADD IMAGE</div>
                <div class="modal-buttons">
                    <button type="button" class="modal-btn" onclick="selectFromGallery()">ADD FROM<br />GALLERY</button>
                    <button type="button" class="modal-btn" onclick="takePhoto()">TAKE A<br />PHOTO</button>
                </div>
            </div>
        </div>

        <script>
            function openImageModal() {
                document.getElementById('imageModal').classList.add('active');
            }

            function closeImageModal() {
                document.getElementById('imageModal').classList.remove('active');
            }

            function selectFromGallery() {
                document.getElementById('galleryInput').click();
                closeImageModal();
            }

            function takePhoto() {
                document.getElementById('cameraInput').click();
                closeImageModal();
            }

            // 파일 선택 시 미리보기 (선택사항)
            document.getElementById('galleryInput').addEventListener('change', function (e) {
                if (e.target.files && e.target.files[0]) {
                    console.log('Image selected from gallery:', e.target.files[0].name);
                }
            });

            document.getElementById('cameraInput').addEventListener('change', function (e) {
                if (e.target.files && e.target.files[0]) {
                    console.log('Photo taken:', e.target.files[0].name);
                }
            });
        </script>
    </body>
</html>
